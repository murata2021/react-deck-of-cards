import React from "react";


const Card=(props)=>{



    return (
        <>
            {/* <img  src={props.pic} style={{transform:`rotate(${props.rotation}deg)`}}/> */}
            <img  src={props.pic} />

        </>
    )
}

export default Card;